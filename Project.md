# VideoSpliter

## Purpose

VideoSpliter is a mobile application whose purpose is to **split a video into individual image frames**.

The app takes a video file as input and produces a set of images extracted from that video.

The focus is on:

- On-device processing
- Simple and predictable behavior
- Offline usage

Nothing more, nothing less.
